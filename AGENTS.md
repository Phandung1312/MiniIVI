# Project context

- This repository contains multiple subprojects.
- `CarSystemUI` and `CarLauncher` are Android system apps.
- Every app in this project must be pushed into the system; do not install apps through standard ADB installation.
- During app implementation, all related files must be 100% in English, including source code, comments, resource text, and documentation.
